---
// src/components/MovieCard.astro
export interface Props {
  title: string;
  poster: string;
  genres: string[];
  rating: number;
  director: string;
  writers: string;
  stars: string;
  releaseDate: string;
  releaseRegion?: string;
}

const {
  title,
  poster,
  genres,
  rating,
  director,
  writers,
  stars,
  releaseDate,
  releaseRegion = 'UK',
} = Astro.props;
---

<div>
  <div style="display: column; align-items: center; text-align: center; padding-top: 150px">
    <img style="border-radius: 25px; position: relative" src={poster} alt={`Poster of ${title}`} />
    <div style="position: relative">
      ⭐ {rating}
    </div>
  </div>

  <div>
    <h2>{title}</h2>

    <div>
      {genres.map((genre) => (
        <span>
          {genre}
        </span>
      ))}
    </div>

    <p><strong>Director:</strong> {director}</p>
    <p><strong>Writers:</strong> {writers}</p>
    <p><strong>Stars:</strong> {stars}</p>

    <p>
      Release Date: {releaseDate} ({releaseRegion})
    </p>
  </div>
</div>
